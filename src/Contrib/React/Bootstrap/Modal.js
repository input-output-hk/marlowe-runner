import { Modal, ModalBody, ModalDialog, ModalFooter, ModalHeader, ModalTitle } from 'react-bootstrap';

export const _Modal = Modal;

export const _ModalBody = ModalBody;

export const _ModalDialog = ModalDialog;

export const _ModalFooter = ModalFooter;

export const _ModalHeader = ModalHeader;

export const _ModalTitle = ModalTitle;

